# Context

MVP needs milk for `cake`. Animals catalog and ANIMAL tiles exist; engine had stubs only.

# Goal

Buy cow on ANIMAL tile, feed with `feed` product, passive milk production in engine.

# Scope

- `BUY_ANIMAL` — server immediate (like shop)
- `FEED_ANIMAL` — server enrich (deduct feed) → engine (reset hunger)
- Passive: `production_elapsed_sec` → milk every `production_interval_sec` if `hunger_ticks` < 40

# Owner

**NIKITA** — server/client  
**SANYA** — engine (stub + C++)

# Status

DONE (2026-05-28)
